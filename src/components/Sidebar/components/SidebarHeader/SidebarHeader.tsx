import React from 'react'

import styles from './SidebarHeader.scss'

export default function SidebarHeader(): PlannerElement {
  return (
    <div className={ styles.sidebarHeader }>
      Online now
    </div>
  )
}
